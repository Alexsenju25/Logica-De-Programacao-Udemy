Algoritmo "tabuada.5"


Var

   n, x, m : inteiro

Inicio

   escreva("Deseja a tabuada para qual valor? ")
   leia(n)
   
   x <- 0
   m <- 0
   para m de 0 ate 9 faca

    m <- m + 1
    x <- n * m
    escreval("4 x", m, " = ", x )
   fimpara


Fimalgoritmo